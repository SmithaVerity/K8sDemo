apiVersion: v1
kind: Service
metadata:
  name: ratings-api-scale
spec:
  selector:
    app: ratings-api-scale
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: ClusterIP
